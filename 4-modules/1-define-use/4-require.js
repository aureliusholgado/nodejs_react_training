const moduleApi = require('./3-wrapper');

console.log(moduleApi);

// this one basically invokes the function the module that is 3-wrapper.js; running this will show how it really is a function by returning its value of module.exports